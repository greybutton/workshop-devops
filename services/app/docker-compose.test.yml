version: '2'

services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: web
  sut:
    build:
      context: .
      dockerfile: Dockerfile.production
    command: make test
    depends_on:
      - db
